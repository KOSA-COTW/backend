package cotw.server.common.log.entity;

public enum AccountEventType {
    USERNAME_LOOKUP_REQUEST,
    EMAIL_VERIFY_REQUEST,
    EMAIL_VERIFY_CONFIRM,
    TOKEN_CONSUME_SUCCESS,
    TOKEN_CONSUME_FAIL
}
